
# Github

```
ssh-keygen -t rsa -b 4096 -C "RolandPf67@googlemail.com" -f  ./id_rsa
```
(Use a passphrase)

Upload public key to github

Clone with ssh-url e.g. "git@github.com:github-pfeifer-syscon-de/... .git", and given key.
